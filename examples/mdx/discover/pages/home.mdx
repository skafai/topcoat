# Auto-Discovery Demo

This example demonstrates **inventory-based component registration** with topcoat-mdx.

The <Highlight>highlight</Highlight> component is registered via `mdx_components!` and submitted to the inventory system.

When the `discover` feature is enabled, components registered this way are automatically available to MDX pages.
